## 1. Project Initialization

- [x] 1.1 Scaffold Vite React TS project (`npm create vite@latest . -- --template react-ts`)
- [x] 1.2 Install dependencies (`npm install`)
- [x] 1.3 Update `tsconfig.json` with path aliases (required by shadcn)

## 2. Tooling Setup

- [x] 2.1 Install Tailwind CSS (`npm install -D tailwindcss postcss autoprefixer`)
- [x] 2.2 Initialize Tailwind (`npx tailwindcss init -p`)
- [x] 2.3 Configure ESLint and Prettier for code formatting

## 3. UI Component Foundation

- [x] 3.1 Initialize shadcn ui (`npx shadcn-ui@latest init` or `npx shadcn@latest init`)
- [x] 3.2 Update `index.css` or `globals.css` with Dark Clean global variables
- [x] 3.3 Set up basic layout components (Header, Main container) structure
