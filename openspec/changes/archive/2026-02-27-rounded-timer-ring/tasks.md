## 1. Modify SVG Stroke
- [x] 1.1 In `TimerDisplay.tsx`, add the `strokeLinecap="round"` attribute to the animating `<circle>` element.
- [x] 1.2 Verify visually in the browser that the progress ring now has rounded ends instead of flat edges as it counts down.
